name: Pipeline Automatizado
on:
  push:
    branches:
      - main
jobs:
  CI-ContinuousIntegration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Make new a .env file
        uses: SpicyPizza/create-envfile@v2.0.3
        with:
          file_name: .env.test
          directory: .
          envs: |
            PORT=5000
            POSTGRES_PASSWORD=151919
            POSTGRES_USER=postgres
            POSTGRES_DB=tests
            DATABASE_URL=postgres://postgres:151919@postgres:5432/tests
      - name: Run tests
        run: docker compose -f docker-compose-test.yml run backend npm run test:ci
